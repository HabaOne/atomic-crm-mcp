# Terraform Backend Configuration
# Copy this file to backend.tfvars and fill in your values
#
# For Hetzner Object Storage (S3-compatible):
# 1. Create an Object Storage bucket in Hetzner Cloud Console
# 2. Create access credentials for the bucket
# 3. Fill in the values below

bucket     = "your-terraform-state-bucket"
key        = "atomic-crm-mcp/dev/terraform.tfstate"
region     = "eu-central-1"

# These can also be provided via environment variables:
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
# access_key = "your-access-key"
# secret_key = "your-secret-key"
